# مطلوب في الإنتاج (Railway → Variables)
SECRET_KEY=استبدل-بقيمة-عشوائية-طويلة

# تخزين دائم — عند إضافة Volume على Railway بمسار /data يُستخدم تلقائياً
# أو عيّن يدوياً:
# DATA_DIR=/data
# DB_PATH=/data/law_office_data.db
# DOCS_DB_PATH=/data/law_office_data_docs.db
# SYSTEM_SETTINGS_PATH=/data/system_settings.json
# BACKUP_ROOT=/data/backups

# محلي (بدون Volume)
DB_PATH=law_office_data.db
DOCS_DB_PATH=law_office_data_docs.db
